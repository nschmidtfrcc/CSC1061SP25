#include <iostream> 

using namespace std; 
   
   /*
   parameter: contain totalSales EP
   prossess: will get called and will display the final total gross sales EP 
   return value: it will return the final gross sale EP 
   */ 
   void displayGrossSales(double totalSales) { // void function to display gross sales EP 
   cout << " total gross sales: " << totalSales << endl; 
           } //end void EP 

   
 int main () { 
 double totalSales = 0.0; 
 
 // switch () {  
   
  // case 5: { 
      /* 
    void sellCar(Car unsoldCars[], Car soldCars[], double &totalSales) {
    string vin;
    int foundIndex = -1;

       if (foundIndex != -1) { // If the car is found
        // Mark car as sold
        unsoldCars[index].setSold(true); 
        totalsales += unsoldCars[index].getPrice();
        */
        //move
        
        for (int i = 0; i < 10; ++i) {
         if(soldCars.getYear[i] == 0) {
            soldCars[i] == unsoldCars[Index]; 
            break; 
            }
          } 
            
        
        /*
        unsoldCars[foundIndex] = Car(); // Reset to a default car object

        cout << "Car with VIN " << vin << " has been sold." << endl;
      */ 
      
      displayGrossSales(totalSales); //calls total gross sales EP
           // break; 
    //  } 

    }//end main EP

 
    